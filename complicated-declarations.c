// pointer to pointer to int
int **p;

// pointer to array of ints
int (*q)[10];

// array of pointers to int
int *n[10];

// function returning pointer to an integer
int *f();

// pointer to function returning an integer
int (*g)();

// pointer to function returning pointer to an integer
int *(*h)();

// function returning pointer to an array of integers
int (*i[10])();

// pointer to function returning pointer to an array of integers
int *(*j[10])()
